# S3 Backup Environment Variables Example
# Copy this file to .env and fill in your values
# Environment variables take precedence over YAML configuration

# Path to YAML config file (optional)
# If not set, only environment variables will be used
S3_BACKUP_CONFIG_FILE=/path/to/config.yaml

# Backup Directories (required if not in YAML)
# Multiple directories separated by commas
BACKUP_DIRS=/Users/username/Documents,/Users/username/Photos,/var/log/myapp

# Recursive backup (optional, default: false)
# Set to "true" to backup subdirectories
BACKUP_RECURSIVE=true

# Cron schedule for automatic backups (optional, default: "0 0 */3 * *")
# Format: "minute hour day month weekday"
BACKUP_CRON_SCHEDULE="0 0 */3 * *"

# AWS Configuration (required)
AWS_REGION=us-west-2
S3_BUCKET=my-backup-bucket

# AWS credentials (if not using AWS CLI configured credentials)
# AWS_ACCESS_KEY_ID=your_access_key
# AWS_SECRET_ACCESS_KEY=your_secret_key
